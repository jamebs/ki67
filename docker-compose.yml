version: '3'
services:
  app:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    shm_size: 8gb
    image: ki67-research:latest
    container_name: ki67-research
    deploy:
      resources:
        reservations:
          devices:
            - capabilities:
              - gpu
    volumes:
      - .:/workspace/ki67
